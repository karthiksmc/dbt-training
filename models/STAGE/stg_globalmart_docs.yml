version: 2

models:
  - name: stg_orders
    description: Its combined all 3 tables
    columns:
      - name: orderid
        description: It is a primary key
      - name: ship_mode
        description: '{{ doc("ship_mode")}}'